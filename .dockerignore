.git
.gitignore
Dockerfile
.dockerignore
devserver.py
README.md
*.md
**/.DS_Store

# Ship only the web-sized media, not the originals.
# (Comments must sit on their own line — Docker treats a trailing "#" as part
#  of the pattern, so an inline one silently matches nothing.)
assets/*.png
assets/*.mp3
